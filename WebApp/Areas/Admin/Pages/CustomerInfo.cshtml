@page
@model WebApp.Areas.Admin.Pages.CustomerInfoModel
@using ServiceLayer
@{
}
<h4>Welcome to the admin area!</h4>
<hr />
<form method="get" id="searchForm">
    <div class="input-group mb-3">
        <input type="search" asp-for="@Model.Search" class="form-control">
        <div class="input-group-append">
            <button class="btn btn-outline-secondary" type="submit">
                <i class="fas fa-search"></i>
            </button>
        </div>
    </div>
    <label asp-for="CustOrderOption">Order product by: </label>
    <select asp-for="CustOrderOption" asp-items="Html.GetEnumSelectList<ProductOrderOptions>()" class="accordion" onchange="searchForm.submit()"></select>
</form>

<table class="table table-striped">
    @foreach (var cust in Model.Customers)
    {
        <partial name="_CustomerListing" model="cust" />
    }
</table>
    <ul class="pagination">
        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            <li class="page-item @(i == Model.CurrPage ? "active" : "")">
                <a asp-page="./Index" class="page-link" asp-all-route-data="@(new Dictionary<string, string>{
        { "Search", "" },
        { "CurrPage", @i.ToString() },
        { "prodOrderOption", Model.CustOrderOption.ToString() }
            })">@i</a>
        </li>
    }
    </ul>
    <a asp-page="./Edit" asp-route-BookId="" class="btn btn-info">Add New</a>
    @section footer {
        @if (Model.Message != null)
        {
            <div class="alert alert-info">@Model.Message</div>
        }
    }
